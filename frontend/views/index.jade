extends layout

block contents
  h3 Queues
  hr

  table.table.table-hover
    thead
      tr
        th Name
        th Queued tasks
        th Processed tasks
        th Failed tasks
    tbody
      if Object.keys(stats).length == 0
        tr
          td(colspan="4") Nothing were found
      else
        each res, elem in stats
          tr
            td
              a(href="/queues/#{stats[elem].qname}")= stats[elem].qname
            td= addCommas(stats[elem].counters.queued)
            td= addCommas(stats[elem].counters.processed)
            td= addCommas(stats[elem].counters.failed)


